# 内容讲解笔记

## IP和Port的基本概念

- IP 地址
  - 223.2.43.222（每个数字的范围：0-255）
- Port 端口号
  - 22：文件传输，SSH（server - 422/24722）
  - 80：http连接
  - 443：https连接
  - 7890：proxy代理（server - 55336）

## git 相关命令

```bash
# 查看当前git的配置
git config --list

# 源控制
## 查看源
git remote -v
## 断开源
git remote remove origin

# 配置基本信息
## 配置账号和邮箱
git config --global user.name "FSPE-NNU"
git config --global user.email "harrisywfw10y@163.com"

### 配置代理
git config --global http.proxy "http://127.0.0.1:55336"
git config --global https.proxy "http://127.0.0.1:55336"

### 配置授权
git config --global credential.helper store

### 默认分支名称
git config --global init.defaultBranch main

### 第一次clone项目的时候需要token，后续不再需要额外配置
```
